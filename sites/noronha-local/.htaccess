RewriteEngine On

# 1. Protege arquivos REAIS (CSS, JS, Imagens, Vídeos)
# Se o arquivo existe fisicamente, o servidor entrega ele sem mexer em nada.
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# 2. Forçar HTTPS e tirar o WWW
# Garante que noronhalocal.com.br seja a URL padrão oficial.
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\. [NC]
RewriteRule ^(.*)$ https://noronhalocal.com.br/$1 [L,R=301]

# 3. Mapear a URL limpa para o arquivo .html
# Quando o usuário clicar em /atalaia-longa, o servidor abre atalaia-longa.html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)$ $1.html [L]